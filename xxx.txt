python -c "import duckdb, pandas as pd; df=pd.DataFrame({'lot_id':['A','B','C'],'order_seq':[1,2,3]}); con=duckdb.connect('duck_test.duckdb'); con.register('df_tmp',df); con.execute('create or replace table lot_test as select * from df_tmp'); con.unregister('df_tmp'); print(con.execute('select * from lot_test order by order_seq').fetchall()); con.close()"
